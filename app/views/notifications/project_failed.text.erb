Hello <%= @project.user.name %>,

  The project "<%= @project.name %>" is now closed. The contributions of <%= number_to_currency(@project.cancelled_amount) %> were insufficient to fund the project for <%= number_to_currency(@project.amount) %>.

Project link: <%= project_url(@project,
                              :host => SETTINGS["mailer"]["host"]) %>
